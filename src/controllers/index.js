const { getComments, createComment } = require('./comments');
const { getPosts, createPost, updatePost } = require('./posts');

module.exports = {
  getComments,
  createComment,
  getPosts,
  createPost,
  updatePost,
};
