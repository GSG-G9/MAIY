const { getComments, createComment } = require('./comments');
const { getPosts, createPosts, updatePost } = require('./posts');

module.exports = {
  getComments,
  createComment,
  getPosts,
  createPosts,
  updatePost,
};
